name:
  singular_underscored: credential
  plural_underscored: credentials
  short: c
description: Data storage credentials
storage:
  fields:
    - name: data_storage_id
      data_type: integer
      constraints:
        - value: not null
    - name: key
      data_type: string
